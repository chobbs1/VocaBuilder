This app will use the AWS eco-system for it's backend

# Amplify - AWS Orchestration Service

# Authentication - Amazon Cognito

# Storage - DynamoDB

# AppSync (GraphQL) - API
`amplify_outputs.json` contains the API configuration and lives at the project root.
- It is **gitignored** — never committed.
- For dev: manually copy it from the Amplify console / backend repo into the project root.
- For CI/CD: automate injection as a build step (future).


# S3 (Storage) - File Storage
Unsure as yet if I need this


